version: '3.8'
services:
  users-api:
    build:
      context: ./users-api
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
  auth-api:
    build:
      context: ./auth-api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
  todos-api:
    build:
      context: ./todos-api
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
  log-processor:
    build:
      context: ./log-message-processor
      dockerfile: Dockerfile
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
