name: Build and Push Docker Images

on:
  push:
    branches:
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build, tag, and push images
      run: |
        # Define a version (e.g., from a GitHub Actions environment variable or hardcoded)
        VERSION=$(date +%Y%m%d%H%M%S)

        # Build and tag images
        docker build -t pipebarreto/log-message-processor:latest -t pipebarreto/log-message-processor:$VERSION ./log-message-processor
        docker build -t pipebarreto/todos-api:latest -t pipebarreto/todos-api:$VERSION ./todos-api
        docker build -t pipebarreto/auth-api:latest -t pipebarreto/auth-api:$VERSION ./auth-api
        docker build -t pipebarreto/users-api:latest -t pipebarreto/users-api:$VERSION ./users-api
        docker build -t pipebarreto/frontend:latest -t pipebarreto/frontend:$VERSION ./frontend

        # Push images to Docker Hub
        docker push pipebarreto/log-message-processor:latest
        docker push pipebarreto/log-message-processor:$VERSION

        docker push pipebarreto/todos-api:latest
        docker push pipebarreto/todos-api:$VERSION

        docker push pipebarreto/auth-api:latest
        docker push pipebarreto/auth-api:$VERSION

        docker push pipebarreto/users-api:latest
        docker push pipebarreto/users-api:$VERSION

        docker push pipebarreto/frontend:latest
        docker push pipebarreto/frontend:$VERSION